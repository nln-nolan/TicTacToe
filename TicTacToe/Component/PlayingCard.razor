@if (Card.IsVisible)
{
    <img class="blackjack-card"
        src="images/cards/@(Card.ImageName).png"
         alt="@(Card.ImageTitle)"
        title="@(Card.ImageTitle)"/>
}
else
{
    <img class="blackjack-card" src="images/cards/cardBack.png" />
}